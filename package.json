{
  "name": "life-dump",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/projects build && bun run --cwd packages/crm build && bun run --cwd packages/mood-tracking build && bun run --cwd packages/food-tracking build",
    "dev": "bun run --cwd packages/core dev & bun run --cwd packages/projects dev & bun run --cwd packages/crm dev & bun run --cwd packages/mood-tracking dev & bun run --cwd packages/food-tracking dev",
    "lint": "biome check .",
    "format": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "typescript": "^5.3.3"
  }
} 
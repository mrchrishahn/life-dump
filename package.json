{
  "name": "life-dump",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "format": "biome format . --write",
    "dev": "bun run --cwd packages/core dev",
    "build": "bun run --cwd packages/core build"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "typescript": "^5.3.3"
  }
} 